if SHIELD_KAKA_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "KAKA"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_KAKA_LEFT || SHIELD_KAKA_RIGHT

config ZMK_SPLIT
    default y

endif